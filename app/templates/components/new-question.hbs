{{#if addNewQuestion}}
<h1>Ask a Question</h1>
  <div>
    <form>
      <div class="form-group">
        <label for="questionAuthor">Name Please!</label>
        {{input value=questionAuthor id="questionAuthor"}}
      </div>
      <div class="form-group">
        <label for="question">Ask Here!</label>
        {{input value=question id="question"}}
      </div>
      <div class="form-group">
        <label for="additionalNotes">Details!</label>
        {{input value=additionalNotes id="additionalNotes"}}
      </div>
      <button {{action 'saveQuestion'}}>Save</button>
    </form>
  </div>
{{else}}
  <button{{action 'questionFormShow'}}>New Question</button>
{{/if}}
