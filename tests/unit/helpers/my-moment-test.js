import { myMoment } from 'messagr/helpers/my-moment';
import { module, test } from 'qunit';

module('Unit | Helper | my moment');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = myMoment([42]);
  assert.ok(result);
});
